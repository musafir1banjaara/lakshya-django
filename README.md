#Creating my first Django Project

-> We learned the basic installations and project setup
-> We learned about the architecture that django foloows (MVT) - Model, View, Template
-> We understood the workflow while working in django peojects i.e. server to the project's url to app url to views.py, views.py would do  
   business logic connect all other stages and finally return the response to data
-> We got an overview of the ORM here, learned ,migrations, working of admin panel etc
-> We used Template tags here
-> render() is a built-in shortcut function that combines a template with a context dictionary and returns a fully formed 
   HttpResponse object
-> In whole we learned the actual working of the Framework

